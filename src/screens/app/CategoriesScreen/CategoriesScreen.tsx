import { Box, Text } from '@components';
import { AppTabScreenProps } from '@routes';

export function CategoriesScreen({}: AppTabScreenProps<'CategoriesScreen'>) {
  return (
    <Box>
      <Text>Categories</Text>
    </Box>
  );
}
